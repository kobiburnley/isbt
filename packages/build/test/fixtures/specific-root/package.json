{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "../../../../../node_modules/.bin/ts-node -T --skip-project ../../../src/bin.ts build",
    "build:a": "yarn build --root a"
  }
}
